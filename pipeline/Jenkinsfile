pipeline {
  agent { 
    label 'helm-slave' 
  }
  stages {
    stage('test') {
      steps {
        sh """
          helm lint --values=helm/artsncrafts/values.yaml helm/artsncrafts/
        """
      }
    }
    stage('helm dry run'){
      steps {
        sh """
          helm init helm/artsncrafts -n dev 
        """
      }
    }
  }
}