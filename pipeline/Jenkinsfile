
pipeline {
  agent {
    label 'helm-slave'
  }
  stages {
    stage('helm linting') {
      steps {
        sh """
          echo $PATH
          which helm
          helm lint --values=helm/artsncrafts/values.yaml helm/artsncrafts/
        """
      }
    }
    stage('helm dry run'){
      steps {
        sh """
          echo $PATH 
          which helm
          helm init helm/artsncrafts -n dev 
        """
      }
    }
  }
}